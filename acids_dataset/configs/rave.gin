
RawParser:
    chunk_length = %CHUNK_LENGTH
    hop_length = %HOP_LENGTH
    pad_mode = "zero_pad"
    sr = %SAMPLE_RATE
    channels = %CHANNELS 

LMDBWriter:
    fragment_class = @AcidsFragment
    parser = @RawParser
    

